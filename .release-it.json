{
  "github": {
    "release": true
  },
  "git": {
    "tagName": "v${version}",
    "commitMessage": "chore: release v${version}"
  },
  "npm": false,
  "plugins": {
    "@release-it/conventional-changelog": {
      "preset": "angular",
      "infile": "CHANGELOG.md"
    }
  },
  "hooks": {
    "before:init": "pnpm lint && pnpm test",
    "after:bump": "node -e \"const fs=require('fs'); [ './package.json', './projects/ngx-theme-stack/package.json' ].forEach(f => { const p=JSON.parse(fs.readFileSync(f, 'utf8')); p.version='${version}'; fs.writeFileSync(f, JSON.stringify(p, null, 2) + '\\n'); })\""
  }
}
